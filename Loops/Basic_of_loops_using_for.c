#include <stdio.h>
int main()
{
    // printf("Hello World");
    for (int i = 1; i <= 4; i += 1)
    {
        // int i=1; is used to initialize the number in the variable i.
        // i<=4 is a condition we are apply in the program.
        // i+=1/i=i+1 is being used for updation of the variable or value.
        printf("Hellow World \n");
    }
    return 0;
}

